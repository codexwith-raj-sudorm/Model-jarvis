<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Voice input -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Alarms & timers (normal permission, granted at install) -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Always-on wake word listener -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Resume an ARMED wake listener after reboot (opt-in via the 👂 toggle) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Later phases (kept commented until used):
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    -->

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jarvis">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Jarvis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Voice-first overlay summoned by the assistant gesture / QS tile / wake word -->
        <activity
            android:name=".assistant.AssistantActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.Jarvis.Assistant" />

        <!-- System Assistant role: lets the user pick JARVIS as the
             "Digital assistant app" (home-swipe / power-hold launch) -->
        <service
            android:name=".assistant.JarvisVoiceService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService.ServiceInterface" />
            </intent-filter>
        </service>

        <!-- Required session host for the interaction service (invisible) -->
        <service
            android:name=".assistant.JarvisSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION_SERVICE" />

        <!-- Always-on "Hey JARVIS" wake word listener -->
        <service
            android:name=".wake.JarvisWakeService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Re-arms the wake listener after reboot, only if the user armed it -->
        <receiver
            android:name=".wake.WakeBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings tile -->
        <service
            android:name=".tile.JarvisTileService"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher_foreground"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- On-demand screen reading: captures ONLY when the screen tool
             asks; events are ignored. User enables it in Settings. -->
        <service
            android:name=".accessibility.ScreenReaderService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>

</manifest>
