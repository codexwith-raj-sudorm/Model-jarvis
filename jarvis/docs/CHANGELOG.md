# JARVIS — Session Log (Canonical Changelog)

> This file is the single source of truth for the project narrative.
> The dossier generator (`scripts/build_dossier.py`) embeds it verbatim into
> §2 of `JARVIS-PROJECT-DOSSIER.md`. **Workflow after every debug/feature
> session:** append an entry at the bottom of this file, then run
> `./dossier.sh` to regenerate the one-file dossier with fresh stats + code.

## Session 1 — Concept
Started as "Sahayak": a 100% offline assistant concept doc (architecture, model tiers, speech stack, memory/RAG, roadmap, license audit). Research snapshot: Qwen3-1.7B best small all-rounder (Apache-2.0), Gemma-3-1B for 4–6 GB phones, Phi-4-mini/Qwen3-4B for flagships.

## Session 2 — Capabilities walkthrough
Mapped exactly what the system can and cannot do (honest limits: no live internet knowledge, not frontier-model quality, device-dependent speed).

## Session 3 — Rename + internet
Renamed to **JARVIS**; redesigned as *local-first hybrid*: opt-in web layer added to the design doc (free sources table, privacy design, "when to go online" router problem).

## Session 4 — Phase 0 build
Full Android scaffold (46 files): Gradle version catalog, manifest, arc-reactor icon, **llama.cpp JNI bridge written against the live `llama.h` API** (fetched and verified that day: `llama_model_load_from_file`, `llama_init_from_model`, `llama_batch_get_one`, sampler chain, `llama_vocab_is_eog`), UTF-8-safe token streaming (byte-level BPE partial-rune buffering), chat templates for 4 model families, agent orchestrator with TOOL_CALL loop, 8 tools, WebFetcher + Readability, SQLite memory, Compose chat UI, model scripts.

## Session 5 — System integration
JARVIS became a real system assistant: `VoiceInteractionService` (+ session service, manifest pattern verified against AOSP docs), translucent voice-first overlay with auto-mic, Quick Settings tile, shared `ChatLog` + warm-engine reuse across screens.

## Session 6 — Voice-first refocus
User: *"main focus should be on connecting to the internet and answering via voice."* Mini-RAG web search (BM25 paragraph ranking — small models can't read whole pages), `news` tool (Google News RSS), `WebIntents` fresh-data router (English + Hindi/Hinglish), voice-optimized answering (short, no markdown, source attribution), `SpeechFormatter`, **hands-free loop** (TTS onDone → mic re-opens), barge-in on tap, web ON by default (persisted). *Engineering note: discovered that parallel edits to the same file had silently clobbered each other in earlier batches; re-verified everything with grep sweeps and re-applied lost edits — all subsequent same-file edits were made strictly sequential.*

## Session 7 — Fully-offline voice (sherpa-onnx)
Verified the real Kotlin API file-by-file from upstream (`OnlineRecognizer.kt`, `OnlineStream.kt`, `Tts.kt`, `FeatureConfig.kt`) and the artifact coordinates from sherpa's own `jitpack.yml` (`com.k2fsa.sherpa.onnx:sherpa-onnx:1.12.40` via JitPack). Built `VoiceInput`/`SpeechOutput` abstractions; `SherpaSttEngine` (AudioRecord 16 kHz → streaming zipformer, endpoint-detected turn taking, live partials) and `SherpaTtsEngine` (Piper/VITS, chunk-streamed via `generateWithCallback` into AudioTrack, abortable = instant barge-in). Auto-selection in `ServiceLocator` with system-engine fallback; `get_voice_models.sh`.

## Session 8 — Wake word
Discovered sherpa's bilingual **KWS zipformer (3M params, int8 encoder ≈ 4.4 MB)** with phoneme-based custom keywords → **"Hey JARVIS" needs zero training**: `JARVIS @JH @AA1 @R @V @AH0 @S` (CMU phonemes, auto-installed). Dual driver (KWS preferred, ASR-phrase fallback), `JarvisWakeService` microphone foreground service (persistent notification, mic handoff to overlay, pause/resume), 👂 UI toggle with POST_NOTIFICATIONS handling on 13+.

## Session 9 — Behavior Q&A
Documented wake standby semantics (manual opt-in, survives app-close/screen-off, not reboot — boot receiver deliberately pending) and barge-in status (tap = instant; speak-over-it parked with rationale: needs AEC/energy-gate, offered as future toggle). Also produced the summon-UI concept ("Arc Reactor HUD", `jarvis-ui-concept.html`) with an interactive mockup + Compose mapping table.

## Session 10 — Improvements inventory → first dossier
P0–P3 roadmap and the single-file record (`JARVIS-PROJECT-DOSSIER.md`) generated by `scripts/build_dossier.py`.

## Session 11 — Concurrency hardening
Two crash-class bugs fixed: (1) *JNI thread safety* — the llama.cpp bridge used unlocked global state, so tapping stop or switching models mid-generation could segfault; every entry point is now serialized behind a `std::mutex`, plus a cooperative `std::atomic` abort flag with a new `nativeStopGenerate()` (stop is instant and callable from the UI thread; load/unload set abort *before* locking so they never wait long; `nativeIsLoaded` reads a lock-free atomic so status checks never block behind a generation). Bonus: a full re-read of `llama_jni.cpp` revealed the `std::string pending` declaration had been lost to the earlier edit race (undeclared identifier — wouldn't have compiled); restored in the rewrite. (2) *Main-thread unblocking* — `SherpaSttEngine.stop()`/`SherpaTtsEngine.stop()` joined their worker threads on the caller (UI) thread for up to ~2 s → stutter/ANR; joins stripped: cleanup now happens on the workers' own threads (`finally` blocks), TTS engine release moved to a daemon thread, the STT mic-open got a 3×200 ms handoff-grace retry, and the wake service's `onDestroy` join was removed for the same reason. `ChatViewModel.stopGeneration()` now calls `engine.stopGenerate()` so the ■ button actually halts the native token loop instead of just cancelling the coroutine.

## Session 12 — Context overflow & UTF-8 hardening
(1) *Mini-RAG text truncation* — web tool results could reach several KB; stacked into history they overflowed the model context and `llama_tokenize` returned a negative count (graceful error, but a dead turn). Three-layer fix: a strict **1500-character clamp on every tool result** at the single point where results enter history (`Orchestrator.MAX_TOOL_RESULT_CHARS` — guards all current and future tools), a **whole-prompt character budget** (8000 chars; history is trimmed oldest-first, always preserving the last 2 turns; sized for Hindi/Hinglish which tokenize at ~2 chars/token on Qwen), and WebSearchTool portioning tuned (top 4 passages × 350 chars) so a normal result lands *under* the clamp instead of being mid-sentence truncated. (2) *UTF-8 stream safety* — the partial-rune buffer (hold bytes until the character completes) already existed from Phase 0; the real remaining gap was malformed input: a stray invalid byte (bad lead, orphaned continuation run, overlong C0/C1, F5+) would still reach `NewStringUTF` and abort the JVM, and the full-result return path had no validation at all. Added `sanitize_utf8()` (drops invalid bytes, stops at partial tails) — now *both* the streaming flush (refactored into one `flush_pending` lambda) and the final return pass through it.

## Session 13 — Verification pass & cross-chat handoff
Project reached a natural pause: scaffold complete, **pre-first-compile** (the authoring environment had no Android SDK). Ran a systematic verification pass (APIs re-checked against upstream llama.h / sherpa-onnx v1.12.40 Kotlin API, permissions vs manifest, service lifecycle semantics), produced the P0–P3 improvements roadmap (P0 = first real compile + in-app model downloader + onboarding; P1 = GBNF tool-calling, citations/fetch-log UI, embedding memory, tests+CI; P2 = SMS/call/calendar tools, screen context, Hindi voice packs, boot auto-start, Arc-Reactor-HUD overlay; P3 = battery/NPU/perf, security, store-readiness) and the first full dossier via `scripts/build_dossier.py`. Packed the cross-chat handoff: `jarvis-project.zip` (full project + git history), `JARVIS-PROJECT-DOSSIER.md` (every source file inlined), `jarvis-github-upload.zip` (no .git, for web upload), `JARVIS-HANDOFF.md` (bootstrap prompt + conventions). Working conventions hardened into the handoff: sequential same-file edits only (edit races corrupted files twice), grep/ast sweeps after every batch, APIs verified against upstream sources — never memory.

## Session 14 — Reconstruction: repo made whole
The GitHub mirror had only 17 flattened highlight files — the full buildable tree existed only in the lost zip/dossier artifacts. Reconstructed the entire project **from the repo's own specs** (the 67-entry manifest in `build_dossier.py`, 12 sessions of changelog engineering detail, the README tuning table, and the 17 ground-truth files). Reorganized into the proper `jarvis/` layout with `git mv` (history preserved), wrote the full Gradle build system (settings/root/app scripts, version catalog with AGP 8.7.3 / Kotlin 2.0.21 / Compose BOM 2024.10 / sherpa-onnx 1.12.40 via JitPack, R8 keep rules, official Gradle 8.9.0 wrapper fetched from the gradle/gradle repo) and all 41 Kotlin sources: LLM layer (engine interface, JNI wrapper with `TokenForwarder` bridging the native `onToken` callback, 4-family chat templates with Gemma role-folding, GGUF model manager), web layer (WebFetcher with 24 h stale-fallback cache + 200-entry access log, Readability, BM25 ParagraphRanker), SQLite memory with lexical top-k, agent layer (lenient TOOL_CALL parser with balanced-brace scanning + regex fallback, registry, WebIntents fresh-data router EN+Hinglish, orchestrator with the 1500-char clamp / 8000-char budget / 3-round cap), 9 tools, speech layer (interfaces, system STT/TTS fallbacks, sherpa streaming STT with endpoint turn-taking and 3×200 ms mic-handoff grace, session-tokenized sherpa TTS with single-thread executor — rapid speak→speak→stop can never double-fire onDone or overlap audio), wake word (KWS + ASR-phrase drivers, microphone foreground service with pause/resume handoff), system integration (VoiceInteractionService + no-op session host, translucent overlay, QS tile), shared ChatLog/ChatViewModel (hands-free loop: TTS onDone → mic re-opens; barge-in on tap; warm-engine model swap) and the Compose UI (arc-reactor dark theme, full chat screen, pulsing overlay). Verification sweeps all green: 8/8 XML valid, TOML/Python AST parse, 41/41 packages match paths, brace balance (Kotlin-aware stripper handling nested `${}` templates), all internal imports resolve, JNI ↔ Kotlin surface exact match, manifest-vs-disk 67/67. sherpa signatures re-verified against the actual v1.12.40 Kotlin API files fetched from upstream (OnlineRecognizer/OnlineStream/Tts/KeywordSpotter/FeatureConfig) per convention #4.

## Session 15 — Code review pass (the reviewer's turn on the reconstruction)
Systematic self-review of the Session-14 reconstruction found and fixed six real defects. (1) **Model path mismatch (critical)**: the scripts adb-push to `/sdcard/Android/data/<pkg>/files/…` (external app-specific storage) but ModelManager/SherpaStt/SherpaTts/KwsWakeDriver all looked in internal `filesDir` — the app would never have found the downloaded models; all four now resolve via `getExternalFilesDir(null) ?: filesDir` (`ModelManager.baseDir`, one shared helper). (2) **Use-after-free windows (crash-class)**: stop()/shutdown() released sherpa native objects on daemon threads while the mic/synthesis worker could still be inside native decode()/write() — every release path now bounded-joins its worker first (`MicLoop.awaitStopped`, `SherpaSttEngine.worker.join(500)`, `SherpaTtsEngine.executor.awaitTermination(500ms)`); joins happen on release threads only, so the Session-11 no-UI-join rule is intact. (3) **Wake ANR**: `JarvisWakeService.startDriver()` ran ONNX init (ASR fallback ≈1–2 s) on the main thread — driver start/stop now run on daemon threads under a `driverLock` with a `wantListening` gate so a stop racing init is honored. (4) **MicLoop had no open retry** — added the same 3×200 ms handoff grace the STT engine has (wake→overlay handoff is exactly where contention lives). (5) **Threading hygiene**: `Orchestrator.template`/`voiceMode` are now `@Volatile` (written from IO, read from Main); the agent turn runs on `Dispatchers.Default` (prompt building hits SQLite — never on Main); `send()` got a CAS guard against double-sends. (6) Cosmetic: dead `WebFetcher.isOnline()` removed, Theme's pointless `isSystemInDarkTheme()` dropped, overlay preview uses `ChatLog.OVERLAY_PREVIEW_TURNS`. *Meta-lesson, again: the parallel same-file edit bug from Session 6 re-manifested during this very session — three parallel edits to WakeDrivers.kt lost one, and a three-edit batch to ChatScreen.kt silently dropped two; all subsequent edits were strictly sequential with per-edit grep verification. Convention #2 is now enforced by scar tissue in two separate sessions.*

## Session 17 — First real build: CI green + the butler upgrades
**The first-compile milestone is crossed.** With no local Android SDK possible in the authoring sandbox (network allowlist blocks everything except GitHub), the "real Android Studio build" moved to GitHub Actions: ubuntu runner + JDK 17 (temurin) + SDK platforms;android-35 + NDK 27.0.12077973 + CMake 3.22.1 running the same `assembleDebug` Studio runs, with the failure tail self-reported as commit comments (Actions blob storage unreachable from the sandbox — api.github.com is not). Build iterations: (1) JitPack's custom-groupId sherpa coordinate 404s → CI fetches the pinned v1.12.40 AAR from the sherpa release into app/libs (build.gradle.kts prefers the local AAR, JitPack stays as the local-dev fallback). (2) Nine first-compile Kotlin errors fixed — headline cause: a `models/*.gguf` path inside a doc comment, because **Kotlin block comments nest**, swallowed the entire ModelManager class (every "Unresolved reference" cascade traced to it); plus a phantom `onShowSession` override (replaced with the documented `VoiceInteractionSession.onShow` + `startAssistantActivity` pattern, verified against developer.android.com), a bare `throw` (Kotlin needs `throw e`), `AudioTrack` float-write needing the 4-arg `WRITE_BLOCKING` overload, a `Unit`-assigned `Thread?` (`.also { it.start() }`), an inferred vararg lambda that can't bind to a val (local `fun has(vararg)`), and dead `arcBorder` helpers from another half-lost edit. (3) **BUILD SUCCESSFUL** — artifact path then fixed twice (ABI-injected builds land in `outputs/apk/<abi>/debug/`, upload now globs `outputs/**/*.apk`), plus an actions/cache on `.cxx` + cmake intermediates keyed to the llama.cpp tag (Phase B — skips FetchContent and makes Ninja incremental). **New tooling: `scripts/audit_fixes.py`** — a canonical inventory of every fix (37 asserts + 7 regression tripwires + a Kotlin-semantics-aware syntax sweep with NESTED comment tracking) that runs before every push and as CI's fail-fast first step; born directly from the silent edit-loss that produced chimera files in Sessions 15–16 (the local dev environment lost edits again during this very session; the audit caught its own regex escaping bug on first run). **Phase C "real JARVIS" upgrades:** C1 butler persona (unflappable, dry wit, "sir"); C2 default voice → `vits-piper-en_GB-alan-medium` (verified in the sherpa tts-models release) at 0.95× rate; C3 daily briefing — app-driven (no LLM round, works pre-model-load): date + home weather + top-3 headlines spoken on the first summon of the morning or the explicit "good morning" intent, sections degrading gracefully offline; C4 in-app model downloader — resumable `WebFetcher.download` (HTTP Range + atomic rename, still the only networking class), curated 3-model catalog, singleton download state that survives the dialog, progress + activate; C5 citations & privacy — the agent stamps which tools produced each answer ("via weather, web_search" chips) and a 🛰 bottom sheet shows every URL fetched this session; C6 boot auto-start for an *armed* wake listener (`WakeBootReceiver`, the user-setting gate Session 9 wanted) + a butler-toned first-run banner ("I lack a brain, sir") routing straight to the downloader. README capability table and roadmap refreshed; llama.cpp pinned to b10876 after verifying all 25 API symbols + exact signatures against the live header (convention #4).

## Session 18 — P0 crossed: green CI, APK artifact, grammar repair, 42 unit tests

The ninth CI run was the charm: **conclusion SUCCESS with a 58.9 MB `jarvis-debug-apk` artifact** (arm64-v8a: Kotlin + llama.cpp JNI b10876 + sherpa-onnx AAR). The winning Locate-APK shape: workspace-wide absolute `find "$GITHUB_WORKSPACE" -name "*.apk"`, workspace-prefix stripped into a step output, upload the exact path. Retrospective on the three prior upload failures: r5's glob was right but the APK path claim wrong; r7's find ran under `working-directory: jarvis` so it searched `jarvis/jarvis/…`; r8's correctly-cwd'd relative find found nothing — the workspace-wide absolute find with prefix stripping is the only variant robust to all of that, and its success implies the relative cwd for find was still not what we assumed (the absolute path is printed in the step log for the archaeologists).

**(1) Builds went full-ABI by default.** With the native cache paying for llama.cpp once, single-ABI fast-path iteration is no longer worth the divergence: every push now compiles arm64-v8a + armeabi-v7a + x86_64. First full run exposed a real portability bug: llama.cpp `sgemm.cpp` uses `vld1q_f16`/`vld1_f16` fp16 NEON intrinsics that don't exist on armv7 — fixed by `GGML_LLAMAFILE OFF` for `armeabi-v7a` only in cpp/CMakeLists.txt (sgemm is a speed fast-path; the standard matmul fallback keeps old budget devices — common in our target region — fully supported).

**(2) P1: GBNF grammar-constrained tool-call repair.** Small models frequently *know* which tool they want but botch the JSON. New repair pass in the agent loop: when free-form output contains `TOOL_CALL` but the lenient parser finds no valid call, re-generate once seeded on the `prompt + "TOOL_CALL "` prefix with a GBNF grammar — `llama_sampler_init_grammar(vocab, str, "root")`, verified against the pinned b10876 `llama.h` before writing a line (fetched fresh; /tmp had been wiped). The sampler physically cannot emit invalid syntax. The pass is silent (no token streaming — it's plumbing, not prose), near-greedy (`temp 0.1`, 160 tokens), string-valued args matching the `Map<String, String>` tool contract. JNI `nativeGenerate` grew an optional `grammar` parameter, appended to the sampler chain *last* so it filters the final candidate set; `LlmEngine.generate` mirrors it with a default of null so no other call site changed.

**(3) P1: 42 pure-JVM unit tests + CI gate.** `testDebugUnitTest` now runs before `assembleDebug` on every push. Coverage targets the components small models abuse most and a rebuild can silently regress: `ToolCallParserTest` (think-blocks, inlined args, nested braces in string values, malformed-JSON fallback, the exact grammar-repair output shape), `ParagraphRankerTest` (BM25-lite ordering, Hinglish tokenization), `SpeechFormatterTest` (markdown/link/citation stripping, word-boundary capping), `WebIntentsTest` (English + Hinglish routing), `BriefingTest`, `ChatTemplateTest` (all four model families + `stripStops`), and `TimeParsingTest` for the alarm/timer natural-language parsers — the flakiest logic in the app, promoted from `private` to `internal` as a deliberate testability seam. `audit_fixes.py` grew a `test/` path namespace (test sources resolve against `app/src/test/…`) — now 48 asserts + 7 tripwires.

**(4) CI hygiene.** A 0-second run failure turned out to be a YAML plain-scalar bug: an unquoted step name containing `: ` ("Unit tests (pure-JVM: parsers, …)") parses as a nested mapping — quoted the name, added local YAML validation to the pre-push ritual (venv + pyyaml; system pip is PEP-668-locked). PR #1 description rewritten around the milestone: green CI, artifact download command, Phase C feature list, remaining P1+ roadmap. Node-20 deprecation warnings on the pinned v4 actions remain cosmetic.

*State at close: P0 complete (installable APK on every green run), GBNF repair + tests landed pending their validating run, dossier regenerated. Next: embedding-based memory (P1), screen context, Hindi voice packs (P2).*

## Session 18b — Voice packs: Hindi voices, in-app download, live switch

**(1) P1 semantic memory shipped** (run 14 pending at write time). **(2) GitHub Actions bumped to current majors** (checkout v7, setup-java v6, cache v6, upload-artifact v7, gradle/actions v6 — Node 24 runtime, deprecation annotations gone). Run 13 was the first fully-green full-ABI run: 42 unit tests + arm64-v8a/armeabi-v7a/x86_64 + a 65 MB artifact — and it validated the armv7 fix (`GGML_LLAMAFILE OFF` for `armeabi-v7a` only; llama.cpp's sgemm.cpp fp16 NEON intrinsics don't exist on armv7).

**Voice packs (P2).** The model downloader gained a second catalog — TTS voices — after verifying the k2-fsa tts-models release: `hi_IN` Hindi voices exist (rohan, priyamvada, pratham; int8/fp16 variants), and a first: sherpa also ships a **Bengali** streaming zipformer ASR (`zipformer-bn-vosk-2026-02-09`) — noted for a future STT upgrade given the user's location. The slice: **VoicePackManager** (new) downloads piper tar.bz2 archives via WebFetcher (still the only networking class; resumable through cache-dir `.part` files), extracts them with commons-compress under `files/voice/tts/<voice-id>/` (zip-slip-guarded, links skipped), and activates in one tap. **SherpaTtsEngine** now resolves the speaking directory: explicit user choice → legacy flat layout (adb-pushed, still works) → any installed pack, deterministic alphabetical; `invalidate()` releases the cached engine so a voice switch applies without an app restart. **ServiceLocator** swaps the live engine on activation (sherpa takes over from system TTS if the new voice checks out). **Orchestrator** reads a reply-language hint — with a Hindi voice active, the system prompt biases replies to Hindi (Devanagari), because an English reply through a Hindi voice sounds wrong. Catalog ships three voices (Alan en_GB default, Rohan + Priyamvada hi_IN, all int8 ≈65 MB). Dependency added: commons-compress 1.27.1 (Maven Central). The downloader dialog's "voice packs still come from scripts/" placeholder is finally true no more.

*State at close: runs 14 (semantic memory) and 15 (action bumps) in flight; voice-pack feature ready for run 16. Next: Bengali/Hindi ASR evaluation, screen context (P2).*

## Session 18c — HUD, comms tools, six straight green runs

Runs 13–18 all green, each publishing a full-ABI (arm64-v8a + armeabi-v7a + x86_64, ~65 MB) artifact: full-ABI matrix with the 42-test gate (13), semantic memory (14), GitHub Actions major bumps to the Node-24 runtime — checkout v7, setup-java v6, cache v6, upload-artifact v7, gradle/actions v6 (15), voice packs with the new commons-compress dependency (16), arc-reactor HUD (17), comms tools (18).

**(1) Arc-reactor HUD.** The overlay reactor was three static circles with a scale pulse; it's now a Canvas-drawn HUD: ten rotating coil segments whose spin speed carries state (26 s idle drift → 3 s generating spin), a counter-rotating 24-segment detail ring, a 36-tick static bezel, a radial glow, three Mark-style core slots counter-rotating inside, and a core whose radius wobbles on two summed frequencies while speaking (calm single-frequency breathing otherwise). Zero new dependencies — pure DrawScope.

**(2) Comms tools — call, SMS, calendar, zero permissions.** All three use intent hand-offs so every action lands one user-confirmation tap away (a hands-free assistant must never misfire a call or message): **CallTool** ACTION_DIAL with a normalized `tel:` number (regex-validated, separators tolerated); **SmsTool** ACTION_SENDTO `smsto:` with the body pre-filled, capped at 480 chars (single-SMS territory); **CalendarTool** ACTION_INSERT with lenient day parsing (`today`/`tomorrow`/`YYYY-MM-DD`) and am/pm times, all-day events anchored at noon to dodge midnight edge cases. Contact-by-name and calendar *reading* deferred — both need runtime permissions with no clean intent fallback. The calendar date math and phone normalization are `internal` and unit-tested (CommsToolsTest).

*State at close: P1 complete (GBNF repair, semantic memory, tests), P2 well ahead of plan (voice packs, HUD, comms). Remaining: screen context (accessibility service — the big one), ASR language packs (sherpa now ships a Bengali streaming zipformer), barge-in AEC, P3 hardening.*

## Session 18d — Screen context (on-demand, privacy-first)

The last big P2 context source: **"what's on my screen, JARVIS?"** A new `ScreenReaderService` (accessibility service, user-enabled in Settings → Accessibility) whose privacy posture is deliberately narrow: it subscribes to window-state events only because the framework requires an event mask to grant window-content access — every received event is ignored in code. Capture happens exclusively when the new **ScreenTool** fires: one walk of `rootInActiveWindow` (320-node / 1500-char budget, duplicate lines collapsed, ≤300-char lines, dead-node tolerant), returned as a flat digest and never stored. If the service is off, the tool says so plainly and the model answers without it. Manifest declares the service non-exported with `BIND_ACCESSIBILITY_SERVICE`; `res/xml/accessibility_service_config.xml` is minimal; the Settings description tells the user exactly what happens (and what keeping it off costs). The tool flows into the system prompt manifest automatically via the ToolRegistry.

## Session 18e — Ears: ASR language packs, Bengali first

The voice-pack pattern now covers speech *input*: an **ASR catalog** in VoicePackManager (shared download/extract core refactored out of the TTS path) with two streaming zipformer packs — the English 2023-06-26 default and **Bengali `zipformer-bn-vosk-2026-02-09`** (83 MB, layout verified against the sherpa docs: `encoder/decoder/joiner.onnx` + `tokens.txt`, zipformer2 — the engine's `firstOnnx("encoder")` prefix matching already handles both naming styles). **SherpaSttEngine** resolves its model directory exactly like TTS now: user's chosen pack → legacy flat layout → any installed pack (deterministic); activation releases the old recognizer (the existing shutdown pattern — bounded join, release on daemon thread) and the next listening session builds from the new directory. The downloader dialog gained an "Ears" section. Very deliberate for the target user: Bhātpāra is Bengal — Bengali ears plus Hindi/English voice and brain cover the region's actual language mix.
